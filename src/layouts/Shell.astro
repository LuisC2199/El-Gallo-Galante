---
import Layout from "./Layout.astro";
import Navigation from "../components/Navigation.astro";
import Footer from "../components/Footer.astro";

const { title = "El Gallo Galante", description, ogImage, ogType } = Astro.props;
---

<Layout title={title} description={description} ogImage={ogImage} ogType={ogType}>
  <div class="min-h-screen flex flex-col selection:bg-stone-200">
    <Navigation />
    <main class="flex-grow pt-24 md:pt-32 pb-20">
      <slot />
    </main>
    <Footer />
  </div>
</Layout>
